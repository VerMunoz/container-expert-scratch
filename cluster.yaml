# Create cluster EKS 

apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig
​
metadata:
  name: clustereks
  region: us-weast-2
​
nodeGroups:
  - name: cluster-vero
    instanceType: t2.small
    desiredCapacity: 1